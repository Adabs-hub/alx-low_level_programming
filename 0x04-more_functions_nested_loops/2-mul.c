#include "main.h"

/**
 * mul - detects digit
 *
 * @a: first arg  takes in a char
 *
 * @b: second arg takes in a chare
 *
 * Return: returns the multiplication of integer chare
 */

int mul(int a, int b)
{
	return (a * b);
}
